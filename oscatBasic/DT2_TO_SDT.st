FUNCTION DT2_TO_SDT
    (*http://www.oscat.de/images/OSCATBasic/oscat_basic333_en.pdf#page=121*)
    RETVAL.YEAR := YEAR_OF_DATE(DI);
    RETVAL.MONTH := MONTH_OF_DATE(DI);
    RETVAL.DAY := DAY_OF_MONTH(DI);
    RETVAL.WEEKDAY := DAY_OF_WEEK(DI);
    RETVAL.MS := UDINT_TO_INT(TOD_TO_UDINT(TI) MOD 1000);
    RETVAL.SECOND := UDINT_TO_INT((TOD_TO_UDINT(TI) / 1000) MOD 60);
    RETVAL.MINUTE := UDINT_TO_INT((TOD_TO_UDINT(TI) / 60000) MOD 60);
    RETVAL.HOUR := UDINT_TO_INT(TOD_TO_UDINT(TI) / 3600000);
    DT2_TO_SDT:=TRUE;
    (* revision history

    hm 18. oct. 2008	rev 1.0
    	original version

    *)
END_FUNCTION
